ignore:
  - "src/tests"

comment:
  layout: "header, diff, tree, changes, sunburst"
  require_changes: false
  
  coverage:
  status:
    patch:
      default:
        target: auto
        threshold: 5
    project:
      default:
        target: auto
        threshold: 5

